"use strict";
// IN FUNCTIONS IT IS ALSO POSSIBLE TO USE INTERSECTIONS
const sum = (x, y) => {
    return (x + y).toString();
};
// VOID = NO RETURN
const log = (message) => {
    console.log(message);
};
const sum1 = (x, y) => {
    return x + y;
};
